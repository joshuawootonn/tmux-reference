---
import "@fontsource-variable/space-grotesk";
import "@fontsource/dm-mono";
import "@fontsource-variable/dm-sans";
import "../styles/global.css";
import { ThemeToggle } from "@/components/theme-toggle";
import { FATHOM_ID } from "astro:env/client";
---

<script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script>

<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>TMUX Reference</title>
    <meta name="description" content="404" />
    <meta name="robots" content="index,follow" />
    <meta name="googlebot" content="index,follow" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@JoshWootonn" />
    <meta property="og:title" content="Josh Wootonn" />
    <meta property="og:description" content="404" />
    <meta property="og:image" content="/seo.png" />
    <meta property="og:image:alt" content="TMUX Reference logo" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="628" />
    <link rel="shortcut icon" href="/favicon.png" />
    <link href="https://twitter.com/JoshWootonn" rel="me" />
    <link rel="sitemap" href="/sitemap-index.xml" />
  </head>
  <div
    class:list={[
      "relative",
      "bg-[image:radial-gradient(var(--pattern-fg)_1px,_transparent_0)] bg-[size:20px_20px] bg-local",
      "grid w-full grid-cols-[repeat(auto-fit,_12px)] gap-0 pb-1",
      "h-screen p-20",
    ]}
  >
    <main
      class:list={[
        "col-span-full",
        "prose dark:prose-invert mx-auto w-full max-w-177",
        "prose-headings:font-(family-name:--font-heading) prose-headings:font-bold",
        "prose-h1:text-6xl",
      ]}
    >
      <div class="not-found -mr-0 mb-5 -ml-0 flex flex-col px-10 py-6">
        <h1 class="!m-0">404</h1>

        <p>
          Whoops! Reach out to <a href="https://www.joshuawootonn.com/"
            >josh wootonn</a
          > if this was unexpected. Otherwise here is a link
          <a href="https://www.joshuawootonn.com/">back to the cheat sheet</a>.
        </p>
      </div>
      <slot />
    </main>
  </div>
  <script
    src="https://cdn.usefathom.com/script.js"
    data-site={FATHOM_ID}
    defer
    is:inline></script>


</html>
